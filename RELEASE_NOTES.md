## 极光音乐 (fnmusic) v1.2.8

### 🚀 新特性与体验优化

1. **🎵 增加下载时歌曲格式选择**
   - 单曲点击下载时弹出音质与格式选择面板，动态展示该歌曲在对应平台实际存在的真实音质档位（标准 128k、高品质 320k、无损 FLAC、母带 Hi-Res 等）。
   - 默认智能推荐并勾选该歌曲当前最高品质，亦可按需切换其他音质与文件格式。
   - 批量下载（如排行榜一键下载全部）默认以最高音质加入后台静默下载队列。
   - 下载抽屉任务列表清晰呈现实际获取到的音质徽标（如 `FLAC`、`320K`、`128K` 等）。
2. **🛡️ 智能降级与死链防护**
   - 优化了第三方音源（如全豆要 qdy.js、星海 xinghai.js）的 FLAC 无损链接失效时的容错处理。
   - 当无损格式请求返回死链（如酷狗、腾讯）时，自动执行同平台音质降级（回退至 320k 或 128k），保障下载任务能够顺利完成。
3. **🔍 动态探测真实可用格式与音频魔数识别**
   - 杜绝硬编码所有格式并盲猜报错的问题，改为通过平台真实数据动态解析可用音质。
   - 后端下载模块集成音频文件头魔数探测（`fLaC` -> `.flac`、`ftyp` -> `.m4a`、`ID3` -> `.mp3` 等），自动校验并纠正文件后缀。
   - 支持多音质格式智能查重，避免重复下载已存在的不同音质歌曲。
4. **🛡️ 免责声明层级优化**
   - 优化免责声明弹窗（z-index 9999），确保首次进入时无论在何种界面均悬浮在最顶层，提示更醒目。

### 📦 安装说明
- 附件中的 `fn-lx-player.fpk` 可直接在飞牛应用中心「手动安装」上传使用。
