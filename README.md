<div align="center">

# 🎵 极光音乐 (Aurora Music) · fnmusic

> 专为 **飞牛私有云 OS (fnOS)** 打造的高保真沉浸式极光音乐播放器与 NAS 本地音乐管理系统。

[![Release](https://img.shields.io/github/v/release/webspider7/fnmusic?color=10b981&label=Release&logo=github)](https://github.com/webspider7/fnmusic/releases)
[![Platform](https://img.shields.io/badge/Platform-fnOS%20%28x86__64%29-3b82f6?logo=linux)](https://developer.fnnas.com/)
[![Vue 3](https://img.shields.io/badge/Frontend-Vue%203%20%2B%20Vite-42b883?logo=vuedotjs)](https://vuejs.org/)
[![Go](https://img.shields.io/badge/Backend-Go%201.22+-00add8?logo=go)](https://go.dev/)
[![License](https://img.shields.io/badge/License-MIT-amber.svg)](LICENSE)

[📦 快速下载 FPK](https://github.com/webspider7/fnmusic/releases/latest) • [✨ 核心功能](#-核心亮点) • [📥 安装指南](#-飞牛-nas-安装使用指南) • [🛠️ 源码构建](#%EF%B8%8F-本地构建与打包) • [⚠️ 免责声明](#%EF%B8%8F-免责声明)

</div>

---

## 🌟 为什么选择极光音乐？

很多 NAS 音乐播放方案依赖庞大的 Docker 容器或臃肿的网页套件，启动慢、占用内存大，并且难以深度融合 NAS 本地目录与现代流媒体听歌体验。

**极光音乐 (fnmusic)** 专为飞牛 NAS 生态定制设计：
- 采用**飞牛原生 FPK 应用架构**（非 Docker），单个静态编译二进制直接在 Debian 底层高效运行；
- 常驻内存仅约 **15MB ~ 25MB**，极低 CPU 负载，零机械硬盘无效唤醒；
- 兼备**黑胶唱机沉浸式视听**、**NAS 本地音乐无损串流**、**多音源自动容灾降级**与**全局后台持久下载**。

---

## ✨ 核心亮点

### 1. 💽 黑胶唱机沉浸式视听空间
- **高颜值交互**：黑胶唱片旋转动效、高保真流光氛围背景、全屏响应式适配（支持手机/平板/电脑大屏）。
- **专业级歌词体验**：支持双语翻译同步对照、字体大小调节、点击歌词任意行快速跳转播放、一键快速复制歌词。
- **多种播放循环模式**：顺序播放、随机播放、单曲循环一键切换。

### 2. 📁 NAS 本地音乐库原盘扫描与串流
- **多格式全兼容**：深度扫描飞牛各存储卷（`/vol1`、`/vol2` 等）中的音频文件（FLAC、APE、WAV、DSD、MP3、AAC、OGG 等）。
- **原盘无损高码率**：支持 HTTP 206 分片流式高保真串流，自动解析歌曲内嵌封面与 ID3 标签，歌词自动关联。
- **存储目录随时随地切换**：内置目录树浏览器，可自由指定歌曲存储与扫描路径。

### 3. 🧩 纯净容器架构 & 自定义第三方音源规则
- **完全开源合规**：本播放器为纯本地播放器容器，**不内置、不捆绑任何版权在线音源解析爬虫**。
- **音源脚本一键导入**：全面兼容标准的第三方音源脚本规则。支持本地 `.js` 脚本上传，或输入网络订阅链接一键导入。
- **秒级自动容灾降级**：可导入多个备用音源，主音源失效或限流时秒级自动无缝降级切换，听歌永不中断。

### 4. 📥 全局独立后台下载管理（切页不中断）
- **持久化下载系统**：下载状态挂载于全局服务，在「发现音乐」、「本地音乐」、「音源管理」之间自由切换，任务绝不中断。
- **静默后台入队**：点击「一键下载全部」或单曲下载，后台静默加入队列，微提示 Toast 友好反馈，不遮挡当前界面。
- **全能控制**：支持全局「全部暂停」、「全部继续」、「停止全部」，单曲任务卡片均提供暂停、继续、重试与取消，支持「已暂停」标签独立分类。

### 5. 🚀 GitHub Release 自动更新检测
- **内置在线版本检查**：直接对接官方 GitHub Release API，自动对比语义化版本号。
- **一键下载升级**：应用内点击即可获取最新 FPK 安装包，更新日志一目了然，保留全部配置与下载歌曲无缝升级。

---

## 📥 飞牛 NAS 安装使用指南

### 方式一：飞牛应用中心手动安装（推荐）

1. **下载最新安装包**：
   前往 GitHub Releases 页面下载最新版：  
   👉 [**下载 fn-lx-player.fpk**](https://github.com/webspider7/fnmusic/releases/latest)
2. **在飞牛应用中心安装**：
   - 登录飞牛 NAS 管理后台，点击进入桌面的**【应用中心】**；
   - 点击界面**左下角**的**【手动安装】**按钮；
   - 选择并上传下载好的 `fn-lx-player.fpk` 文件；
   - 确认安装，等待几秒即可在飞牛桌面看到全新的**「极光音乐」**图标！
3. **升级更新**：
   - 后续有新版本时，直接在应用内「更新」弹窗下载新版 FPK，重复上述手动安装步骤直接覆盖安装，所有音源与数据完全保留。

---

## 🎧 音源规则导入指引

由于极光音乐为纯净本地播放器，初次使用时如需试听网络曲库，需导入第三方音源脚本：

1. 打开应用，点击顶部导航栏的**【音源管理】**；
2. **导入方式 A（网络订阅）**：在「在线导入」输入框中填入公开的音源订阅链接（例如 `http://.../latest.js`），点击导入；
3. **导入方式 B（本地文件）**：点击「本地上传脚本」，选择保存到本地的 `.js` 音源脚本文件上传；
4. 导入成功后，在列表中将该音源设为激活状态，返回「发现音乐」即可畅享网络搜索、榜单与精选歌单推荐。

---

## 🛠️ 本地构建与打包

如果您希望自行修改代码并重新打包生成 FPK：

### 前置要求
- **Go 1.22+**（需安装并配置好环境变量）
- **Node.js 18+** 与 **npm**
- **fnpack**（飞牛官方打包工具，位于 `scripts/fnpack.exe` 或 Linux 下对应可执行文件）

### 一键构建打包 (Windows PowerShell)

在项目根目录下运行一键打包脚本：
```powershell
powershell -ExecutionPolicy Bypass -File .\build_and_pack.ps1
```

该脚本将自动完成：
1. `frontend` 编译并生成静态资源（Vite build）；
2. 交叉编译 Linux amd64 原生 Go 二进制文件（`GOOS=linux GOARCH=amd64`）；
3. 校验并组装 `fpk-package/app` 应用目录与桌面元数据；
4. 调用 `fnpack build` 生成最终的 `fn-lx-player.fpk`；
5. 解包验证 `app.tgz` 内部可执行程序与图标完整性。

---

## 📂 工程目录结构

```text
fnmusic/
├── frontend/                     # Vue 3 前端工程源码
│   ├── src/
│   │   ├── components/           # UI 组件 (播放器/下载抽屉/更新弹窗/NAS浏览等)
│   │   ├── engine/               # 极光音源运行时沙箱 (lx-runtime)
│   │   └── services/             # 全局下载管理器 (downloadManager)
│   └── vite.config.js
├── backend/                      # Go 原生高性能后端
│   ├── pkg/
│   │   ├── api/                  # HTTP 路由与静态资源服务 (嵌入前端 dist)
│   │   ├── charts/               # 排行榜与歌单聚合接口
│   │   ├── downloader/           # 本地持久化分块下载器
│   │   ├── nas/                  # NAS 目录扫描与音频流式服务
│   │   ├── sources/              # 自定义音源管理
│   │   └── updater/              # GitHub Releases 自动更新检测器
│   └── main.go                   # 后端主入口
├── fpk-package/                  # 飞牛原生应用标准打包目录
│   ├── manifest                  # 应用标识、版本、端口与桌面入口描述
│   ├── cmd/                      # 生命周期启动/停止/升级回调脚本
│   └── app/                      # 核心程序与桌面 UI 配置
└── build_and_pack.ps1            # 自动化全链路构建打包脚本
```

---

## ⚠️ 免责声明

1. **纯粹的技术研究与本地管理容器**：本软件是一款专为 NAS 环境打造的本地音频播放器和个人音乐文件管理工具，其本身不包含、不托管、不提供任何音频资源的搜索、上传或播放直链。
2. **规则自愿原则**：软件内所有关于网络曲库的检索与播放功能均依赖用户自主导入的第三方音源规则。任何音源脚本均由第三方独立维护，其版权与法律责任归属于各脚本作者及各内容平台。
3. **非商业用途**：本开源项目仅供技术交流与个人学习使用，严禁用于任何形式的商业用途。

---

## 📄 开源协议

本项目基于 [MIT License](LICENSE) 协议开源。
